import Subheading from '../../components/Subheading/Subheading.tsx';
import StepTabs from '../../components/StepTabs/StepTabs.tsx';
import HTMLInstallation from '../../code_sections/installation/html/HTMLInstallation.tsx';
import HTMLConfig from '../../code_sections/installation/html/HTMLConfig.tsx';
import HTMLUsers from '../../code_sections/installation/html/HTMLUsers.tsx';
import HTMLLocation from '../../code_sections/installation/html/HTMLLocation.tsx';

import ReactInstallation from '../../code_sections/installation/react/ReactInstallation.tsx';
import ReactConfig from '../../code_sections/installation/react/ReactConfig.tsx';
import ReactAuth from '../../code_sections/installation/react/ReactAuth.tsx';
import ReactAuthContactList from '../../code_sections/installation/react/ReactAuthContactList.tsx';
import ReactAuthLogoutUser from '../../code_sections/installation/react/ReactAuthLogoutUser.tsx';
import ReactDocument from '../../code_sections/installation/react/ReactDocument.tsx';
import ReactLocation from '../../code_sections/installation/react/ReactLocation.tsx';


import { NextSeo } from 'next-seo';

# 1. Install [#install]

<NextSeo 
  title="Velt Docs - Installation"
  description="Installation documentation page for the Velt SDK"
/>

export function InstallSteps() {

  const frontendOptions = {
    'React': <ReactInstallation sectionId="install" />,
    'Next.js': <ReactInstallation sectionId="install" />,
    // 'HTML': <HTMLInstallation sectionId="install" />
  };

  return (
    <StepTabs 
      frontendOptions={frontendOptions}
    />
  );
}

<InstallSteps />

{/* <br />
## Advanced Configurations (optional) [#configure]
**The <code>SnippylyProvider</code> component takes a <code>config</code> property.**

You can pass in a configuration object that affects the SDK at a global level across your application.
<br />
export function ConfigSteps() {
  const frontendOptions = {
    'React': <ReactConfig sectionId="configure" />,
    'Next.js': <ReactConfig sectionId="configure" />,
    // 'HTML': <HTMLConfig sectionId="configure" />
  };

  return (
    <StepTabs 
      frontendOptions={frontendOptions}
    />
  );
}

<ConfigSteps /> */}

# 2. Authenticate [#authenticate]
**Autheticate your logged in user with the SDK.**

{/* As soon as you get your authenticated user, you should call the SDK's `identify()` method. */}

<br />

export function AuthSteps() {
  const frontendOptions = {
    'React': <ReactAuth sectionId="authenticate" />,
    'Next.js': <ReactAuth sectionId="authenticate" />,
    // 'HTML': <ReactAuth sectionId="authenticate" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<AuthSteps />

{/* ## Enable @ mentions & user tagging [#user-contact-list]
For some features like Comments, your users should be able to @mention or tag other users. <br />
You can enable autocomplete and show a contacts dropdown by using this feature.
<br />
export function AuthContactListSteps() {
  const frontendOptions = {
    'React': <ReactAuthContactList sectionId="user-contact-list" />,
    'Next.js': <ReactAuthContactList sectionId="user-contact-list" />,
    // 'HTML': <ReactAuthContactList sectionId="user-contact-list" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<AuthContactListSteps /> */}


{/* ## Additional options [#additional-options]
export function AuthLogoutUserSteps() {
  const frontendOptions = {
    'React': <ReactAuthLogoutUser sectionId="additional-options" />,
    'Next.js': <ReactAuthLogoutUser sectionId="additional-options" />,
    // 'HTML': <ReactAuthLogoutUser sectionId="additional-options" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<AuthLogoutUserSteps /> */}


# 3. Initialize Document [#initialize-document]
Document is the space where you want your users to collaborate. <br />
For a design tool it's the canvas, for a video editor, it's the editor, for a documentation tool, it's the document etc.<br />
A document is like a virtual house.
Users in the same virtual house can see and interact with each other.
<br />
export function InitDocumentSteps() {
  const frontendOptions = {
    'React': <ReactDocument sectionId="initialize-document" />,
    'Next.js': <ReactDocument sectionId="initialize-document" />,
    // 'HTML': <ReactDocument sectionId="initialize-document" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<InitDocumentSteps />

{/* ## Initialize Location (optional) [#location]
A location is like a room in the virtual house. <br />
This represents a space inside the main `document` that you set in the previous step. <br />
Depending on the app, this could represent slides, sections, popups, versions, frames (in video editor), data points on maps/charts etc.
<br />
export function InitDocumentLocationSteps() {
  const frontendOptions = {
    'React': <ReactLocation sectionId="initialize-location" />,
    'Next.js': <ReactLocation sectionId="initialize-location" />,
    'HTML': <ReactLocation sectionId="initialize-location" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<InitDocumentLocationSteps /> */}
