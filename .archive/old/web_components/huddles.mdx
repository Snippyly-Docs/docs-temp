# Huddle

_Communicate with your teammates without leaving the browser._

---

import { Huddle } from '@snippyly/snippyly-demo-component'
import { documentID } from '../../../components/documentID'

export const huddleDocumentId = documentID("huddle");

<Huddle user={"Patrick"} documentId={huddleDocumentId}/>

## Adding the Huddle Tool